// import { BaseGenerator } from "../base.generator";

// export function ModuleGenerator(id: string, moduleTemplates: { name: string; path: string; content: string }[]) {
// 	for (const template of moduleTemplates) {
// 		BaseGenerator(id, template, true);
// 	}
// }
